/* tslint:disable:no-unused-variable */

import { RunRepositoryService } from './run-repository.service';

describe('Service: RunRepository', () => {
  // beforeEach(() => {
  //   addProviders([RunRepositoryService]);
  // });
  //
  // it('should ...',
  //   inject([RunRepositoryService],
  //     (service: RunRepositoryService) => {
  //       expect(service).toBeTruthy();
  //     }));
});
